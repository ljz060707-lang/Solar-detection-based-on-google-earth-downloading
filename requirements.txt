# 创建虚拟环境（必做，避免冲突）
conda create -n solar python=3.10 -y
conda activate solar

# 安装稳定依赖（修复rasterio/geopandas/torch报错）
conda install -c conda-forge rasterio geopandas gdal shapely pycocotools -y
pip install torch torchvision opencv-python pyyaml pillow tqdm
